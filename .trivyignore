# Trivy vulnerability exceptions (spec 006 / T002, FR-002).
# Only upstream-UNFIXABLE findings, each justified and time-bounded. Never add a
# fixable CVE here; remediate the dependency instead. todos-api currently has
# ZERO exceptions: bumping express 4.21, express-jwt 8, body-parser 1.20, redis
# 3.1, prom-client 15 and dropping the unused prometheus-client cleared all
# fixable HIGH/CRITICAL. Keep this file empty of CVEs.
